# Reference material safety

The production bundle intentionally does not include copied community executable scripts.

Community reference copies were moved to:

`/home/ubuntu/leo/artifacts/image-generation-skills-archive-20260427/references/community-skills/`

Treat them as reference-only. Do not execute them for active skill work. Some upload prompts/images to third-party APIs, require external credentials, or read local session logs. Private images require explicit user approval before any external upload.
